<p id="notice"><%= notice %></p>

<p>
  <strong>Email:</strong>
  <%= @customer.email %>
</p>

<p>
  <strong>Name:</strong>
  <%= @customer.name %>
</p>

<% if @reservation %>
  <p>
    <strong>Rental Charge:</strong>
    <%= @reservation.rental_charge %>
  </p>

  <% if @reservation.status == 0 %>
    <p>
      <%= link_to 'My reserved car', @reservation.car %>
    </p>
  <% end %>
<% end %>

<%= link_to 'Edit Profile', edit_customer_path(@customer) if session[:user_type]==0 %>
<br><br>
<%= "Function: " %>

<%= link_to "Rent Cars", cars_path %>|

<%= link_to 'Checkout history', "#{reservations_path}?task=1,customer=#{@customer}" %>

<% if [1,2].include?(session[:user_type]) %>
|<%= link_to "Back", customers_path %>
<% end %>